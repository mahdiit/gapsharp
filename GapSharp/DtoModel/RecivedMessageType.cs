namespace GapSharp.DtoModel
{
    public enum RecivedMessageType
    {
        join,
        text,
        image,
        video,
        audio,
        file,
        voice,
        triggerButton,
        invoiceCallback,
        payCallback,
        submitForm
    }
}